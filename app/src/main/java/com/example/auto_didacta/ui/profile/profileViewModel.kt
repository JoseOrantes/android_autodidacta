package com.example.auto_didacta.ui.profile

import androidx.lifecycle.ViewModel
import com.example.auto_didacta.data.profileRepository

// ViewModel for Profile screen; repository default initialized
class profileViewModel(private val repository: profileRepository = profileRepository()) : ViewModel() {
	// add profile-related logic here later
}