package com.example.auto_didacta.ui.register

import androidx.lifecycle.ViewModel
import com.example.auto_didacta.data.registerRepository

// ViewModel for Register screen; repository default initialized
class registerViewModel(private val repository: registerRepository = registerRepository()) : ViewModel() {
	// add register-related logic here later
}